SUPABASE_URL=<your-supabase-url>
SUPABASE_KEY=<your-supabase-anon-key>
SUPABASE_MEAL_IMAGES_BUCKET=meal-images
VITE_CONTACT_MAIL=<your-contact-email>

# Production/Demo Settings
NUXT_PUBLIC_SIGNUP_DISABLED=false
NUXT_PUBLIC_DEMO_EMAIL=demo@mealsnap.app
# DEMO_PASSWORD is server-only (NOT NUXT_PUBLIC_*). The browser never sees this value.
# The /api/auth/demo-login endpoint uses it server-side to sign in the demo user.
DEMO_PASSWORD=<your-demo-password>

# Server-only Supabase service role key (bypasses RLS — never expose to the browser).
# Must NOT be prefixed with VITE_ or NUXT_PUBLIC_. The CI security-check workflow
# enforces this: see .github/workflows/security-check.yml.
# Set NUXT_SUPABASE_SERVICE_KEY in your deployment environment (Vercel/Railway/etc.).
NUXT_SUPABASE_SERVICE_KEY=<your-supabase-service-role-key>

# Note: TensorFlow.js works locally without any API keys!

# The public URL of your deployed app.
# Used by the CSRF middleware to validate the Origin header on mutation API requests.
# In development this defaults to http://localhost:3000.
# In production set this to your real domain (e.g. https://mealsnap.app).
NUXT_PUBLIC_APP_URL=http://localhost:3000